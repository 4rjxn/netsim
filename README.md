# Netsim is a network simulation build using graph.

This in build as part of the data structures lab. I really liked this 
assignment actually its the only one with some level decency other assignments are just 
ai slopes.


> Not even a single line of code is generated, everything is typed by me.